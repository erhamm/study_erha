import React from "react";

const XuniList = () => {
  return (
    <div>
      <XnList itemHeight={30} containerHeight={100} renderItem={renderItem} />
    </div>
  );
};

export default XuniList;

const XnList = () => {
  return;
};
